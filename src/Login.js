import React, { Component } from "react";
import Header from "./Components/Header";

export class Login extends Component {
  render() {
    return (
      <div>
        <Header />
        <p>This is where users log in</p>
      </div>
    );
  }
}

export default Login;
